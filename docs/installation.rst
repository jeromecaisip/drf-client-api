============
Installation
============

At the command line::

    $ easy_install api_clients

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv api_clients
    $ pip install api_clients
